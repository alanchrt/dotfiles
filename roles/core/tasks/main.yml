---
- name: Install core packages
  become: yes
  apt:
    name:
      - build-essential
      - software-properties-common
      - python-is-python3
      - regolith-look-dracula # TODO move to i3 role
      - ansible
      - curl
      - git
      - vim
      - byobu
      - ranger
      - ripgrep
      - jq
      - cmake
      - imagemagick
      - whois
      - ldnsutils
      - markdown
      - acpi
      - unzip
      - i3xrocks-battery # TODO move to i3 role
    update_cache: yes

- name: Insure local bin path exists
  file:
    path: "{{ ansible_env.HOME }}/.local/bin"
    state: directory

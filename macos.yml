---
- hosts: all
  roles:
    - { name: terminal, tags: [terminal] }
    - { name: nodejs, tags: [nodejs] }
    - { name: ios, tags: [ios, dev] }
    - { name: docker, tags: [docker, dev] }
    - { name: fonts, tags: [fonts] }
    - { name: rectangle, tags: [rectangle, wm] }
    - { name: vim, tags: [vim] }
    - { name: emacs, tags: [emacs] }
    - { name: github, tags: [github] }
    - { name: aws, tags: [aws, dev] }
    - { name: choose, tags: [choose] }
    - { name: claude-code, tags: [claude-code] }
    - { name: graphite, tags: [graphite] }

    # keep completions last since they depend on programs to generate
    - { name: completions, tags: [completions] }

---
- name: Install system basics
  become: yes
  apt: name={{item}}
       state=installed
       update_cache=true
  with_items:
    - build-essential
    - ntp
    - ranger
    - tree
    - htop
    - zip
    - unzip
    - silversearcher-ag
    - jq
    - httpie
    - whois

- name: Start ntp
  become: yes
  service: name=ntp
           state=started

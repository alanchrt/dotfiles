---
- name: Provision dev system
  hosts: all
  roles:
    - base
    - git
    - zsh
    - byobu
    - {role: emacs-source, when: ansible_distribution_release == "trusty"}
    - {role: emacs, when: ansible_distribution_release != "trusty"}
    - vim
    - python
    - latex
    - node
    - postgres
    - mysql
    - sqlite
    - nfs
    - heroku
    - aws
    - ngrok
    - mfa

- name: Configure dev environment
  hosts: all
  roles:
    - dotfiles
